import { DefaultProps } from "@/lib/default-props";

export async function CoursesPostsSection({ dictionary, lang }: DefaultProps) {
    return (
        <p>
            HOLA! this is the courses posts sections
        </p>
    )
}
